apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Reference the base configuration
resources:
  - ../../base
  - secrets-local.yaml
  - slm-resource-limit.yaml

# Patches for local Docker Desktop deployment
patches:
  - path: deployment-patches.yaml