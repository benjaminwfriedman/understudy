apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: understudy
spec:
  volumeClaimTemplates:
  - metadata:
      name: postgres-storage
    spec:
      accessModes: ["ReadWriteOnce"]
      storageClassName: default  # Azure disk storage
      resources:
        requests:
          storage: 10Gi